# CLAUDE.md — Frontend Design System

> 이 파일은 본 프로젝트의 **시각적 비율, 리듬, 톤**을 정의합니다.
> 구체적인 색상값·폰트명·브랜드 자산이 아닌 **구조적 디자인 원리**를 담습니다.
> 모든 코드 작성 전 이 파일을 읽고, 위배되는 출력은 즉시 폐기 후 재작성할 것.

---

## 0. 디자인 철학 — 한 단어

**Restraint.** (절제)

더 많이 보여주려 하지 않는다. 한 화면에 큰 시각 요소 하나, 짧은 한마디, 그리고 여백. 그것이 전부다.

다음 다섯 가지가 모든 의사결정의 기준이다:

1. **Quiet Confidence** — 시각적으로 시끄럽지 않다. 작게 말하되 단호하다.
2. **Editorial Calm** — 잡지 펼침면의 호흡. 여백이 콘텐츠만큼 의미를 가진다.
3. **Mono with One Voice** — 거의 모노톤. 액센트는 단 하나, 매우 작은 면적.
4. **Motion as Atmosphere** — 모션은 정보가 아니라 분위기다. 절대 산만하지 않다.
5. **Cinematic, not decorative** — 영화의 한 컷처럼. 장식이 아니라 분위기.

---

## 1. 절대 금지 — 즉시 폐기 사유

### 형태 위반
- ❌ 중앙 정렬 hero — 좌측 정렬 또는 좌하단 정렬이 기본
- ❌ 균등 3-column / 4-column 카드 그리드
- ❌ 둥근 카드(`border-radius: 8px+`) + drop shadow 조합
- ❌ 화려한 그라데이션 배경
- ❌ 글래스모피즘 (`backdrop-blur` + 반투명 흰색)
- ❌ 카드 안에 또 카드, 박스 안에 또 박스 (nested containers)

### 밀도 위반
- ❌ 한 viewport에 3개 이상의 시각적 초점
- ❌ 섹션 간 여백이 콘텐츠보다 작은 답답한 구조
- ❌ 텍스트가 viewport 폭 90% 이상 차지 (제목 제외)
- ❌ 균등하게 흩뿌려진 micro-interaction 남발

### 톤 위반
- ❌ 트렌디한 컬러 그라데이션 (보라→핑크 등)
- ❌ 자동재생 영상에 소리
- ❌ 화려한 spring 바운스 모션
- ❌ 식상한 폰트 단독 사용 (Inter, Arial, system-ui)

---

## 2. 비율 시스템 — 핵심

### 2-1. Hero 영역 비율

```
┌─────────────────────────────────────────┐
│                                         │
│                                         │  ← 상단 ~25%: 숨 쉬는 공간
│                                         │
│                                         │
│                                         │
│  ┌──────────────────┐                   │  ← 텍스트 블록은
│  │ Big Headline     │                   │     좌측 1/3 ~ 1/2 영역
│  │ Sub copy here    │                   │
│  │ → cta            │                   │
│  └──────────────────┘                   │
│                                         │  ← 하단 ~15%: 여백
└─────────────────────────────────────────┘
```

- Hero 높이: **100vh** (또는 최소 85vh)
- 텍스트 블록 폭: viewport의 **35~50%**
- 우측 50~65%: **의도적으로 비워둔다** 또는 영상이 차지
- 텍스트 수직 위치: 좌하단 또는 좌중간 (절대 중앙 X)

### 2-2. 콘텐츠 폭 비율

```
viewport 100%
├── 좌우 패딩: clamp(5%, 4vw, 8%)        ← 좌우 호흡
├── 콘텐츠 영역: 100% - (패딩 × 2)
│   ├── 본문 max-width: 약 38~55ch       ← 한글/영문 가독성
│   ├── 제목 max-width: 약 12~22ch       ← 강제 줄바꿈
│   └── 풀블리드 미디어: 100%
```

### 2-3. 수직 리듬 (Vertical Rhythm)

```
섹션 ─────────────
        │
        │  내부 여백:
        │    제목 → 본문: 본문 line-height × 2~3
        │    본문 → CTA:  본문 line-height × 2
        │
섹션 ─────────────
        │
        │  섹션 간 여백:
        │    최소: 본문 line-height × 8
        │    기본: 본문 line-height × 12
        │    호흡: 본문 line-height × 16+
        │
섹션 ─────────────
```

**원칙: 섹션 간 여백 ≥ 섹션 내부 여백 × 3**

여백이 답답하면 디자인 전체가 무너진다. 의심스러우면 늘려라.

---

## 3. 타이포그래피 비율

### 3-1. 폰트 페어링 구조

세 가지 역할로 분리:

| 역할 | 특성 | 사용처 |
|------|------|--------|
| **Display** | 캐릭터가 강한 산세리프 또는 세리프. 큰 크기에서 진가 발휘 | Hero 제목, 섹션 제목 |
| **Body** | 한글 가독성 최우선. 한영 자연스러운 혼용 | 본문, 캡션, 메뉴 |
| **Mono** | tabular-nums 지원. 데이터 정렬 시 사용 | 숫자, 메타데이터, 라벨 |

**규칙:**
- Display + Body 2-폰트 페어링이 기본 (Mono는 필요 시 추가)
- 한글 본문은 가독성 좋은 한글 산세리프 우선, 영문 폰트는 한글 fallback 앞에 둠
- Display는 weight 400~600, Body는 weight 400 기본
- 모든 폰트는 variable font 또는 다중 weight 로드

### 3-2. 타입 스케일 — 점프율

**선형 증가 금지. 극단적 대비를 통한 위계 강조.**

| 단계 | 비율 (기준 1.0) | 용도 |
|------|----------------|------|
| caption | 0.75 ~ 0.875 | 캡션, 메타 |
| body | **1.0** | 본문 (16~18px) |
| body-lg | 1.125 ~ 1.25 | 강조 본문 |
| h4 | 1.5 | 작은 제목 |
| h3 | 2.0 ~ 2.5 | 섹션 부제 |
| h2 | 3.0 ~ 4.0 | 섹션 제목 |
| h1 / hero | **5.0 ~ 8.0** | Hero 제목 |
| display | **8.0 ~ 12.0** | 시그니처 타이틀 |

**핵심: 본문(1.0) → Hero(5.0+) 사이의 점프가 5배 이상이어야 한다.**

소심한 점프(1.0 → 1.5 → 2.0 → 2.5)는 평범함의 지름길. h2와 hero 사이는 의도적으로 비워둔다.

### 3-3. 타이포그래피 디테일 비율

| 속성 | 작은 텍스트 | 본문 | 큰 제목 (40px+) | Hero (80px+) |
|------|------------|------|----------------|--------------|
| letter-spacing | 0 ~ +0.01em | 0 | **-0.02em ~ -0.03em** | **-0.03em ~ -0.04em** |
| line-height | 1.4 | **1.6 ~ 1.7** (한글) | 1.1 ~ 1.15 | **0.95 ~ 1.05** |
| max-width | 폭 무관 | 38~55ch | 22ch | **12~16ch** |
| text-wrap | normal | pretty | balance | balance |
| 가능하면 | — | — | 단어별 줄바꿈 의도 제어 | `<br>` 직접 |

**핵심 비율 원칙:**
- 큰 텍스트일수록 **자간을 좁히고** (negative letter-spacing)
- 큰 텍스트일수록 **행간을 좁힌다** (line-height 작게)
- 큰 텍스트일수록 **한 줄 폭을 좁힌다** (max-width ch 적게)

---

## 4. 컬러 구조 — 값이 아닌 위계

구체 HEX는 프로젝트마다 정의. 다음 **구조와 면적 비율**은 고정.

### 4-1. 면적 비율: 60 - 35 - 5

```
█████████████████████████████████  Bg (60%)        — Off-white 또는 near-black
████████████████                   Fg / Lines (35%) — Foreground 텍스트 + 디바이더
█                                  Accent (5% 이하) — 단 하나의 액센트
```

### 4-2. 위계 구조

```
배경
├── primary bg          ← 페이지 기본 배경 (off-white 또는 near-black)
└── elevated bg          ← 카드/모달 (배경과 미세 차이)

전경 (텍스트)
├── primary fg           ← 본문, 제목
├── secondary fg         ← 부제, 보조 정보
├── muted fg             ← 캡션, 메타데이터
└── faint fg             ← 비활성, 플레이스홀더

선
├── line                 ← 일반 디바이더
└── line-strong          ← 강조 디바이더

액센트
└── single accent        ← 오직 하나. 링크/핵심 CTA/데이터 강조에만
```

### 4-3. 운용 원칙

- **거의 흑백.** 베이스는 무채색 또는 매우 낮은 채도
- **순수 흰색/검정 금지.** off-white (살짝 따뜻한 톤) / near-black (살짝 푸른/푸근한 톤)
- **액센트는 한 화면에 1~3개소** 이하. 면적 5% 절대 초과 X
- **액센트를 배경 면적으로 쓰지 마라.** 텍스트 색/보더/점 강조 정도가 한계
- **라이트 섹션과 다크 섹션이 교차**하며 페이지 리듬을 만든다

---

## 5. 레이아웃 비율

### 5-1. 핵심 원칙: 풀블리드 섹션 스택

페이지 = 세로로 쌓인 풀블리드 섹션의 시퀀스. 각 섹션은 viewport 폭 100%를 차지.

```
┌─────────────────────────┐
│  Section 1 (Hero, Dark) │  100vh
├─────────────────────────┤
│  Section 2 (Light)      │  ~80vh
├─────────────────────────┤
│  Section 3 (Dark, Media)│  100vh
├─────────────────────────┤
│  Section 4 (Light)      │  auto
└─────────────────────────┘
```

**규칙:**
- 모든 섹션은 `width: 100vw`
- 콘텐츠는 섹션 내부의 좌우 패딩 안에서 배치
- 섹션마다 톤 (라이트/다크) 교차로 리듬 형성
- 균일한 높이의 섹션이 5개 이상 반복되면 실패

### 5-2. 정렬 규칙

| 요소 | 정렬 |
|------|------|
| Hero 텍스트 | **좌측 정렬, 좌하단 또는 좌중간 배치** |
| 섹션 제목 | 좌측 정렬 |
| 본문 | 좌측 정렬 |
| 캡션/라벨 | 좌측 정렬 (또는 우측 정렬 — 잡지 캡션 스타일) |
| CTA | 좌측 정렬, 인라인 |
| 통계/숫자 강조 | 좌측 정렬 가능, 단독으로 큰 크기 사용 |

**중앙 정렬은 금지. 사용 시 명확한 의도 필요.**

### 5-3. 비대칭 그리드

대칭 그리드(1:1, 1:1:1) 지양. 다음 비율 권장:

- `5fr 7fr` — 작은 라벨/캡션 + 큰 콘텐츠
- `3fr 9fr` — 메타 영역 + 메인 영역
- `1fr 2fr` — 좌측 텍스트 + 우측 미디어 (또는 반대)
- 황금비 근사: `1fr 1.618fr`

모바일에서는 모두 1-col로 stack.

### 5-4. 둥근 모서리

- 기본: **0px** (직각)
- 허용: 최대 4px (작은 인풋, 칩)
- 큰 카드/이미지에 8px+ radius 금지

---

## 6. 미디어 비율

### 6-1. Hero 미디어

- **풀스크린 풀블리드** — 100vw × 100vh
- 가로형 16:9 또는 21:9 풀스크린이 기본
- 모바일에서는 9:16 또는 1:1 버전 별도 준비

### 6-2. 영상 운용 (필수 속성)

```html
<video autoplay muted loop playsinline preload="metadata"
       poster="…" aria-hidden="true">
  <source src="…" type="video/mp4" />
</video>
```

- `muted`: 무조건. 소리 있는 자동재생 절대 금지
- `playsinline`: iOS 인라인 재생
- `loop`: 끊김 없는 반복
- `poster`: 로드 전 정지 이미지
- `aria-hidden`: 장식 영상은 스크린 리더 제외
- `prefers-reduced-motion: reduce`: 정적 이미지로 대체

### 6-3. 영상 콘텐츠 톤

- 느린 카메라 무빙
- 시네마틱 색보정 (살짝 desaturated)
- 8~15초 짧은 루프
- 사람/풍경/이동 등 분위기 중심, 제품 클로즈업 X

### 6-4. 영상 위 텍스트

가독성을 위해 둘 중 하나:

```css
/* 옵션 1: 어두운 그라데이션 오버레이 */
::after {
  background: linear-gradient(180deg,
              rgb(0 0 0 / 0.1) 0%,
              rgb(0 0 0 / 0.5) 100%);
}

/* 옵션 2: 텍스트 자체에 큰 blur 그림자 */
text-shadow: 0 2px 24px rgb(0 0 0 / 0.4);
```

### 6-5. 정적 이미지 비율

| 컨텍스트 | 비율 |
|---------|------|
| Hero (영상 대체) | 16:9 또는 21:9 |
| 풀블리드 섹션 | 16:9 ~ 4:3 |
| 인용/포트레이트 | 4:5 또는 3:4 |
| 카드 (사용 시) | 3:2 또는 4:5 |

---

## 7. 모션 — Cinematic, Restrained

### 7-1. Easing 톤

| 토큰 | 값 | 느낌 |
|------|----|----|
| `ease-out-cinema` | `cubic-bezier(0.16, 1, 0.3, 1)` | 끌어당기듯 부드럽게 감속 — 메인 reveal |
| `ease-in-out-soft` | `cubic-bezier(0.65, 0, 0.35, 1)` | 양방향 부드러운 호흡 |
| `ease-out-quick` | `cubic-bezier(0.33, 1, 0.68, 1)` | 호버용 빠른 감속 |

**금지:** `ease` 기본값, `cubic-bezier(.68,-.55,.27,1.55)` 같은 bounce overshoot.

### 7-2. Duration 비율

| 토큰 | ms | 용도 |
|------|----|----|
| instant | 100 | 즉각 피드백 |
| quick | 200 | 호버, 포커스 |
| base | 350 | 표준 전환 |
| slow | 700 | 섹션 reveal |
| cinema | 1200 | Hero reveal, 큰 모먼트 |

**규칙: 큰 요소일수록 느리게. Hero 제목은 1초 이상이 자연스럽다.**

### 7-3. 모션 패턴 우선순위

**필수 1: Scroll-Triggered Section Reveal**
- viewport 진입 시 `opacity: 0 → 1`, `y: 40 → 0`
- duration 700~900ms, ease-out-cinema
- 페이지당 가장 중요한 모션

**권장 2: Word-by-Word Title Reveal**
- 큰 제목은 단어 단위로 50~80ms 간격 stagger
- 작은 텍스트에는 적용 금지 (산만함)

**권장 3: Counter Animation**
- 큰 숫자 강조는 0 → 실제값 1.5~2초 count-up
- `tabular-nums` 필수

**호버:**
- 링크: underline `transform: scaleX(0 → 1)` left origin
- 이미지: 미세 zoom (`scale(1.04)`) 700ms
- 버튼: 색 invert만, 크기 변화 X

### 7-4. 절대 원칙

- **한 페이지에 high-impact 모션은 1~2회**로 집중
- **scattered micro-interaction 남발 금지**
- **`prefers-reduced-motion: reduce` 무조건 처리**
- **모션은 콘텐츠가 아니라 분위기다** — 모션이 주인공이 되면 실패

---

## 8. 컴포넌트 형태 언어

### 8-1. CTA / 링크

```
인라인 텍스트 링크 + 화살표 → 가 기본 형태
강조 CTA는 직각 보더 박스 (둥근 모서리 X)
패딩 비율: 좌우 1.5 : 상하 1 (가로로 더 넓음)
hover 시 배경/전경 invert (단순한 두 상태 토글)
```

### 8-2. 네비게이션

- 상단 고정, 좌측 로고 + 우측 메뉴
- 초기 배경 transparent → 스크롤 시 솔리드 배경
- 모바일은 햄버거 → **풀스크린 오버레이** (drawer 아님)
- 메뉴 항목 폰트 weight 500, size sm~base

### 8-3. 카드 (가급적 지양, 사용 시)

- **그림자 X, 둥근 모서리 X**
- 1px 보더 또는 배경 톤 차이로만 분리
- 내부 좌측 정렬, 큰 여백
- hover 시 보더 색 또는 미세 brightness만 변경

### 8-4. 리스트 (카드 그리드의 대체)

```
─────────────────────────────────
[Label]   Title here              Date
─────────────────────────────────
[Label]   Another title           Date
─────────────────────────────────
```

- 세로로 쌓인 리스트, 1px 디바이더로 구분
- 호버 시 좌측에 점/화살표 등장 또는 보더 색 변경

### 8-5. 푸터

- 다단 링크 그리드 + 회사 정보
- 좌측 정렬, 폰트 size sm
- 1px 라인으로 영역 구분

### 8-6. 데이터 표현 (통계/카운터)

```
┌─────────────────────┐
│  1,234,567          │  ← 큰 숫자 (Mono, tabular-nums)
│  units              │  ← 작은 라벨 (muted)
└─────────────────────┘
```

- 숫자와 라벨의 크기 비율 **5:1 이상**
- viewport 진입 시 count-up
- 액센트 색 적용 가능 (절제해서)

---

## 9. 반응형 비율

```
모바일:   ~640px
태블릿:   641~1024px
데스크탑: 1025px+
```

**모바일 변환 원칙:**
- 모든 비대칭 그리드 → 1-col stack
- 풀스크린 영상 → 9:16 또는 1:1 버전 사용
- 좌우 패딩은 viewport 비율로 (5%~6%)
- 큰 제목은 `clamp()` 기반으로 부드럽게 축소 (단, 작아도 임팩트 유지)
- 햄버거 메뉴 → **풀스크린 오버레이** (사이드 drawer 금지)

**핵심: 모바일이 데스크탑의 단순 축소가 되면 안 된다.** 모바일에서도 같은 톤과 호흡이 유지되어야 한다.

---

## 10. 접근성

- WCAG 2.1 AA 색 대비 (본문 4.5:1, 큰 텍스트 3:1)
- 키보드 포커스 visible — 디자인된 focus ring (`outline: 2px solid` + offset)
- 시맨틱 HTML 사용 (`<section>`, `<nav>`, `<article>`)
- 모든 의미 있는 이미지에 alt 텍스트
- 자동재생 영상은 `aria-hidden="true"` + 무음 + reduced-motion 대체
- 본문 바로가기 (skip-to-content) 링크 제공
- 텍스트 크기는 rem/em 기반 (200% 확대 시 가로 스크롤 없어야 함)

---

## 11. 작업 워크플로우

### 새 페이지/섹션 작업 시

1. **톤 결정** — 라이트 섹션인가 다크 섹션인가, 풀블리드 영상 hero인가 텍스트 본문인가
2. **비율 결정** — 텍스트 폭은 viewport 몇 %인가, 좌측에서 몇 %부터 시작하는가
3. **시맨틱 마크업** — `<section>`, `<article>`, `<nav>` 등 의미 있는 태그
4. **토큰만으로 1차 완성** — 임의 색/사이즈 금지, 시스템 토큰만 사용
5. **여백/리듬 조정** — 섹션 간/내부 여백 위 비율 따라 검증
6. **미디어 배치** — 영상은 placeholder 시작, 실제 자산 후 교체
7. **모션 추가** — 최소한. scroll reveal + hover만
8. **반응형 검증** — 모바일/태블릿/데스크탑 실측
9. **접근성 패스** — 키보드 탭, 포커스 링, alt, reduced-motion

### 셀프 체크리스트

작업 완료 전 답할 수 있어야 함:

- [ ] **여백이 콘텐츠만큼 많은가?** (적으면 답답함)
- [ ] **한 viewport에 시각적 초점이 1~2개로 압축되었는가?**
- [ ] **좌측 정렬을 지켰는가?** (중앙 정렬 hero 금지)
- [ ] **본문(1.0)과 hero(5.0+) 사이의 점프가 5배 이상인가?**
- [ ] **모노톤 베이스 + 액센트 5% 이하인가?**
- [ ] **라이트/다크 섹션이 교차하며 리듬을 만드는가?**
- [ ] **둥근 모서리/그림자/카드 그리드를 사용하지 않았는가?**
- [ ] **자동재생 영상이 무음이고 reduced-motion 대체가 있는가?**
- [ ] **모션이 cinematic하고 절제되어 있는가?** (산만하지 않은가)
- [ ] **모바일에서도 같은 톤이 유지되는가?**

---

## 12. 최종 원칙

> **"Less, but better placed."**
>
> 채우는 디자인이 아니라 비우는 디자인.
> 무언가 추가하고 싶은 충동이 들면 먼저 빼는 쪽을 고려할 것.
> 큰 시각 요소 하나, 짧은 텍스트 하나, 그리고 충분한 여백.
> 그것이 이 시스템의 전부다.

---

_이 가이드는 비율과 리듬에 관한 것이다. 구체적인 색상, 폰트, 자산은 각 프로젝트의 정체성에 맞춰 별도로 정의할 것._
