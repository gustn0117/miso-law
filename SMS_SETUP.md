# SMS 알림 셋업 가이드 (Solapi · CoolSMS)

> 신규 상담 접수 시 자동으로 운영자(010-9885-7010)에게 문자 알림이 가도록 하는 절차입니다.
> **사업자등록증 불필요**, 개인 신용/체크카드 1장만 있으면 됩니다.

---

## 1. Solapi 가입 (약 3분)

1. https://console.solapi.com/signup 접속
2. 이메일 가입 (Google 로그인도 OK) — 사업자등록증 X, 본인 명의면 됨
3. 가입 후 자동 로그인

## 2. 발신번호 등록 (약 5분)

신규 가입 시 신규 회원 무료 100건 크레딧이 지급됩니다.

1. 좌측 메뉴 **`발신번호`** → **`발신번호 등록`** 클릭
2. **개인 휴대폰**으로 등록 권장 (사업자등록증 없이 가능)
   - 등록 방식: **휴대폰 인증** 선택
   - 휴대폰 번호: `010-9885-7010` (대표자 김학찬 명의)
   - SMS 인증번호 입력 → 즉시 등록 완료
3. 등록 후 상태가 **"등록완료"** 가 되면 발신 가능

> ⚠️ 본인 명의가 아닌 번호(예: 회사 대표번호)는 통신서비스 이용증명원 PDF 업로드 필요.

## 3. API Key 발급 (약 1분)

1. 좌측 메뉴 **`개발자센터`** → **`API Key 관리`**
2. **`새 API Key 만들기`** 클릭
3. 이름은 자유 (예: `miso-law-production`)
4. 권한은 **`메시지 발송`** 만 체크 (최소 권한)
5. 발급 후 **`API Key`** 와 **`API Secret`** 두 줄을 **즉시 복사** (Secret은 발급 후 다시 못 봄)

## 4. 결제수단 등록 (약 2분)

1. 좌측 **`충전·결제`** → **`결제수단 관리`**
2. 카드 등록 → 자동충전 권장
   - **자동충전 임계값**: 잔액이 1,000원 미만일 때
   - **자동충전 금액**: 10,000원씩 (단문 ~1,200건)
3. 또는 수동 충전 (10,000원 = SMS ~1,190건, 한 번 충전으로 수개월 사용 가능)

> 💰 가격: SMS 8.4원/건 · LMS 32.7원/건 · 카카오 알림톡 8원/건

## 5. 서버 환경변수 등록 (저에게 4줄만 알려주세요)

위 3, 4번이 끝나면 아래 4개 값을 저에게 알려주세요. 서버 docker-compose.yml에 즉시 반영하고 컨테이너 재기동까지 처리합니다.

```
SOLAPI_API_KEY=NCSXXXXXXXXXXXXX
SOLAPI_API_SECRET=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
SOLAPI_FROM=01098857010
SOLAPI_TO_ADMIN=01098857010
```

- `SOLAPI_FROM` — 위 2단계에서 등록한 발신번호
- `SOLAPI_TO_ADMIN` — 알림 받을 번호. 콤마로 여러 명 가능 (예: `01098857010,01011112222`)

---

## 발송될 메시지 예시

```
[미소법률] 신규상담 #142
대출/회생 · 김OO
010-1234-5678
```

- 한글 ~30자 이내 → SMS (8.4원)
- 초과 시 자동 LMS (32.7원, 한 번에 1,000자까지)
- 발신: 등록된 010-9885-7010 (수신자에게 평소 번호로 보임)

## 동작 방식

- 상담 접수 → DB 저장 → 이메일(SMTP) + SMS(Solapi) **병렬** 발송
- 환경변수 4개 중 하나라도 비어 있으면 SMS는 **조용히 스킵** (사이트 정상 동작)
- 발송 실패 시 서버 로그(`docker logs miso-law`)에만 기록되고 사용자 응답에는 영향 없음

## 테스트 방법

설정 완료 후:
1. 사이트 → 무료 상담 신청 → 본인 정보로 1건 제출
2. ~3초 안에 `010-9885-7010`으로 문자 도착 확인
3. 관리자 페이지 → 신규 접수 카드에도 동시에 표시됨
